{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-6 offset-md-3">
        <h1 class="mb-4">Mot de passe oublié</h1>

        <p>Indiquez l'adresse email utilisée lors de votre inscription. Si un compte existe, un lien de réinitialisation sera généré.</p>

        <div class="card">
            <div class="card-body">
                {{ form_start(requestForm) }}
                    <div class="mb-3">
                        {{ form_row(requestForm.email) }}
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary">Envoyer le lien de réinitialisation</button>
                    </div>
                {{ form_end(requestForm) }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_login') }}">Retour à la page de connexion</a>
        </div>
    </div>
</div>
{% endblock %}


