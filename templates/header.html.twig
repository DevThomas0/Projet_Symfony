<nav class="backdrop-blur bg-slate-900/80 border-b border-slate-800 sticky top-0 z-20">
    <div class="max-w-5xl mx-auto px-4 py-3 flex items-center justify-between gap-4">
        {# Brand #}
        <a href="{{ path('app_home') }}" class="flex items-center gap-2 group">
            <span class="h-8 w-8 rounded-xl bg-primary-600/80 shadow-lg shadow-primary-600/40 flex items-center justify-center text-xs font-bold tracking-tight">
                MS
            </span>
            <span class="text-sm font-semibold tracking-wide text-slate-100 group-hover:text-primary-200 transition-colors">
                Mon Site
            </span>
        </a>

        {# Menu Items #}
        <div class="hidden md:flex items-center gap-5 text-xs text-slate-300">
            <a href="{{ path('app_home') }}" class="hover:text-primary-300 transition-colors">Accueil</a>
            <a href="{{ path('app_contact') }}" class="hover:text-primary-300 transition-colors">Contact</a>
            {% if app.user %}
                <a href="{{ path('app_service_index') }}" class="hover:text-primary-300 transition-colors">Services</a>
                <a href="{{ path('app_user_profile') }}" class="hover:text-primary-300 transition-colors">Mon Profil</a>
            {% endif %}
        </div>

        {# Right side (Auth) #}
        <div class="flex items-center gap-3 text-xs">
            {% if app.user %}
                <span class="hidden sm:inline-flex items-center px-3 py-1 rounded-full bg-slate-800/80 text-slate-200 border border-slate-700">
                    Bonjour, <span class="ml-1 font-semibold">{{ app.user.username }}</span>
                </span>
                <a href="{{ path('app_logout') }}"
                   class="inline-flex items-center px-3 py-1.5 rounded-md bg-primary-600 text-white font-medium shadow-sm
                          hover:bg-primary-500 active:bg-primary-700 transition-colors text-xs">
                    Déconnexion
                </a>
            {% else %}
                <a href="{{ path('app_login') }}"
                   class="text-slate-300 hover:text-primary-300 transition-colors hidden sm:inline-block">
                    Connexion
                </a>
                <a href="{{ path('app_register') }}"
                   class="inline-flex items-center px-3 py-1.5 rounded-md bg-primary-600 text-white font-medium shadow-sm
                          hover:bg-primary-500 active:bg-primary-700 transition-colors text-xs">
                    Inscription
                </a>
            {% endif %}
        </div>
    </div>
</nav>
